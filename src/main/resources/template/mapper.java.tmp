package ${mapperPackage-};

${importPackage-}

public interface ${mapperClass-} {

    ${entityClass-} getById(${idClass-} ${idProp-});

    boolean insert(${entityClass-} ${entityName-});

    boolean update(${entityClass-} ${entityName-});

    boolean delete(${idClass-} ${idProp-});
}